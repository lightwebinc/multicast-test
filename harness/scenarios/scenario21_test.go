package scenarios

import "testing"

// Scenario 21 — Subtree group ramp (24.5h ramp test)
//
// SKIP_ALWAYS in bash run-all too. Long-running ramp test.
func TestScenario21_SubtreeGroupRamp(t *testing.T) {
	t.Skip("24.5-hour ramp test — SKIP_ALWAYS in bash run-all")
}
